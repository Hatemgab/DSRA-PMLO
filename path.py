# This file is used to check the Python path and ensure that the necessary libaries can be imported correctly.
# It can be run to verify that the environment is set up properly for the project.
import sys
for path in sys.path:
    print(path)